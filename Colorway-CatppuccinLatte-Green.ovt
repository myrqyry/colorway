@OBSThemeMeta {
    name: 'Catppuccin Latte — Green';
    id: 'com.myrqyry.Colorway.CatppuccinLatte.Green';
    extends: 'com.myrqyry.Colorway';
    author: 'myrqyry';
    dark: 'false';
}

@OBSThemeVars {
    /* Light theme: selected-item text stays dark */
    --text_inverse: #191724;

    /* Replay/VirtualCam active state accent (derived from --primary) */
    --accent_bg_start: rgba(64, 160, 43, 0.3);
    --accent_bg_end: rgba(64, 160, 43, 0.1);

    --primary: #40a02b;
    --primary_light: #179299;
    --primary_lighter: #40a02b;
    --primary_dark: #209fb5;
    --primary_darker: #acb0be;

    /* Icon colors (currentColor SVGs, controlled via CSS color) */
    --ico: var(--text);
    --ico_selected: #eff1f5;
}
