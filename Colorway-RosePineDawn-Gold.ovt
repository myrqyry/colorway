@OBSThemeMeta {
    name: 'Rose Pine Dawn — Gold';
    id: 'com.myrqyry.Colorway.RosePineDawn.Gold';
    extends: 'com.myrqyry.Colorway';
    author: 'myrqyry';
    dark: 'false';
}

@OBSThemeVars {
    /* Light theme: selected-item text stays dark */
    --text_inverse: #FFFFFF;

    /* Replay/VirtualCam active state accent (derived from --primary) */
    --accent_bg_start: rgba(234, 157, 52, 0.3);
    --accent_bg_end: rgba(234, 157, 52, 0.1);

    --primary: #ea9d34;
    --primary_light: #d7827e;
    --primary_lighter: #907aa9;
    --primary_dark: #b4637a;
    --primary_darker: #cecacd;

    /* Icon colors (currentColor SVGs, controlled via CSS color) */
    --ico: var(--text);
    --ico_selected: #faf4ed;
}
