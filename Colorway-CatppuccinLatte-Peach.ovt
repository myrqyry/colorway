@OBSThemeMeta {
    name: 'Catppuccin Latte — Peach';
    id: 'com.myrqyry.Colorway.CatppuccinLatte.Peach';
    extends: 'com.myrqyry.Colorway';
    author: 'myrqyry';
    dark: 'false';
}

@OBSThemeVars {
    /* Light theme: selected-item text stays dark */
    --text_inverse: #191724;

    /* Replay/VirtualCam active state accent (derived from --primary) */
    --accent_bg_start: rgba(254, 100, 11, 0.3);
    --accent_bg_end: rgba(254, 100, 11, 0.1);

    --primary: #fe640b;
    --primary_light: #df8e1d;
    --primary_lighter: #dc8a78;
    --primary_dark: #e64553;
    --primary_darker: #acb0be;

    /* Icon colors (currentColor SVGs, controlled via CSS color) */
    --ico: var(--text);
    --ico_selected: #eff1f5;
}
