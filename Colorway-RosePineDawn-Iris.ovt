@OBSThemeMeta {
    name: 'Rose Pine Dawn — Iris';
    id: 'com.myrqyry.Colorway.RosePineDawn.Iris';
    extends: 'com.myrqyry.Colorway';
    author: 'myrqyry';
    dark: 'false';
}

@OBSThemeVars {
    /* Light theme: selected-item text stays dark */
    --text_inverse: #FFFFFF;

    /* Replay/VirtualCam active state accent (derived from --primary) */
    --accent_bg_start: rgba(144, 122, 169, 0.3);
    --accent_bg_end: rgba(144, 122, 169, 0.1);

    --primary: #907aa9;
    --primary_light: #d7827e;
    --primary_lighter: #56949f;
    --primary_dark: #286983;
    --primary_darker: #cecacd;

    /* Icon colors (currentColor SVGs, controlled via CSS color) */
    --ico: var(--text);
    --ico_selected: #faf4ed;
}
